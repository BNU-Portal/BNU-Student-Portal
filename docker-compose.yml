version: '3.9'

services:
  api:
    image: bnu-student-portal-api
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bnu-student-portal-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=db;Database=BNUStudentPortalDb;User Id=sa;Password=Your_password123;TrustServerCertificate=True
    depends_on:
      - db
    networks:
      - bnu-student-portal-network

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: bnu-student-portal-db
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=Your_password123
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql
    networks:
      - bnu-student-portal-network

volumes:
  sqldata:

networks:
  bnu-student-portal-network:
    driver: bridge
